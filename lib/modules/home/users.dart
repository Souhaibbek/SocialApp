import 'package:flutter/material.dart';

class UsersScreens extends StatelessWidget {
  const UsersScreens({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Users'),
      ),
    );
  }
}
